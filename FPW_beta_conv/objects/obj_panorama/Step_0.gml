/// @desc

/*
x = mouse_x_real_offset();
y = mouse_y_real_offset();

camera_set_view_pos(view_camera[view_current], 
	floor(sin(time() * 2) * 12 + 0.5), floor(cos(time() * 1) * 12 + 0.5))

image_xscale = sin((time() / 1)) * 3;
image_yscale = cos((time() / 2)) * 3;
*/
